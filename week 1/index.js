const name = "Abraham";
let surname = "John";

const fullname = name + " " + surname;

// alert(fullname);

// Tasking for week one

const user = prompt(
  "Welcome To Flexisaf Week one Task Please enter your name"
).toLowerCase();
const FirstLetter = user.slice(0, 1).toUpperCase();
const concats = user.slice(1, user.length);

alert("Welcome to Flexisaf " + FirstLetter + concats);

// Life in weeks challenge
function lifeinweekschallenge(age) {}
